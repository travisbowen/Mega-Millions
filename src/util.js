


// Functions that we will probably need
// Date converter from 2021-02-16T00:00:00.000 to
export const dateConverter = (Data) => {
    console.log("hi");
    Data.map((item) => {
        console.log(item.draw_date[0]);
    });
}
// Filter by all time

// Filter by year

// Filter by month

